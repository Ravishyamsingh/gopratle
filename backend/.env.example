# MongoDB Atlas connection string (do not commit your actual value)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<database>?retryWrites=true&w=majority
# Comma-separated origins are supported. Use http://localhost:3000 locally.
CORS_ORIGIN=http://localhost:3000
PORT=5000
